package com.jarvis.framework.core;

import org.junit.jupiter.api.Test;

class LogicResultTest {

    @Test
    public void test() {

        /*List<String> successList = new ArrayList<>();

        List<Object> failureList = new ArrayList<>();

        LogicResult<BatchProcessResult<String, Object>> result = LogicResult.successWithBatchProcess(successList, failureList);

        LogicResult<BatchProcessResult<String, Object>> result1 = LogicResult.failureWithBatchProcess(successList, failureList);
        if (result.getSuccess()) {
            BatchProcessResult<String, Object> data = result.getData();
            List<String> successList1 = data.getSuccessList();
            List<Object> failureList1 = data.getFailureList();
        }*/

        Integer i = 1;

        Double d = (double) i;
    }




}
